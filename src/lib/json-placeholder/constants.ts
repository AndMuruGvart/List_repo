/** Public JSONPlaceholder API used for demo data. */
export const JSON_PLACEHOLDER_BASE = "https://jsonplaceholder.typicode.com";

/** Matches `_limit` in the assignment (10 posts per page). */
export const POSTS_PER_PAGE = 10 as const;
